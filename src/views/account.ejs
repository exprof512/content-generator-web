<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Личный кабинет - ContentGen Hub</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/css/style.css">
    <script>
        window.API_BASE_URL = "<%= API_BASE_URL %>";
        tailwind.config = {
            darkMode: 'class',
            theme: { extend: { fontFamily: { sans: ['Inter', 'sans-serif'] } } }
        }
    </script>
</head>
<body class="bg-gray-50 dark:bg-gray-900">
    <div class="min-h-screen flex flex-col items-center justify-center p-4">
        <div class="w-full max-w-md bg-white dark:bg-gray-800 rounded-2xl shadow-xl p-8">
            <div class="flex flex-col items-center">
                <div id="account-avatar-wrapper" class="w-24 h-24 rounded-full mb-4 border-4 border-purple-500 bg-purple-500 text-white flex items-center justify-center text-4xl font-bold overflow-hidden"></div>
                <h1 id="account-name" class="text-2xl font-bold text-gray-900 dark:text-white"></h1>
                <p id="account-email" class="text-gray-500 dark:text-gray-400"></p>
            </div>

            <div class="mt-8 space-y-4">
                <div class="p-4 bg-gray-100 dark:bg-gray-700 rounded-lg">
                    <p class="text-sm text-gray-500 dark:text-gray-400">Текущий тариф</p>
                    <p id="account-tariff" class="text-lg font-semibold text-gray-900 dark:text-white"></p>
                </div>
                <div class="p-4 bg-gray-100 dark:bg-gray-700 rounded-lg">
                    <p class="text-sm text-gray-500 dark:text-gray-400">Осталось генераций</p>
                    <p id="account-generations" class="text-lg font-semibold text-gray-900 dark:text-white"></p>
                </div>
                <div class="p-4 bg-gray-100 dark:bg-gray-700 rounded-lg">
                    <p class="text-sm text-gray-500 dark:text-gray-400">Подписка активна до</p>
                    <p id="account-expires" class="text-lg font-semibold text-gray-900 dark:text-white"></p>
                </div>
            </div>

            <div class="mt-8 flex flex-col space-y-2">
                <a href="/pricing" class="w-full text-center px-6 py-3 bg-purple-600 hover:bg-purple-700 text-white rounded-lg font-semibold transition-colors">
                    Управление подпиской
                </a>
                <button id="account-logout-button" class="w-full text-center px-6 py-3 text-gray-600 dark:text-gray-300 rounded-lg hover:bg-gray-200 dark:hover:bg-gray-700 transition-colors">
                    Выйти
                </button>
            </div>
        </div>
        <a href="/" class="mt-6 text-purple-600 hover:text-purple-800 dark:text-purple-400 dark:hover:text-purple-300">← Вернуться в чат</a>
    </div>

    <script src="/js/account.js"></script>
</body>
</html>